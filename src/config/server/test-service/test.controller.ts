const SERVICE = 'test-service'
const CONTROLLER = 'testController'

export default  {
   /**
   * 获取用户菜单
   */
  getUserMenu: {
    service: SERVICE,
    controller: CONTROLLER,
    action: 'getUserMenu',
    type: 'GET'
  },
}